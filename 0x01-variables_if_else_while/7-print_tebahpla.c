#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print lowercase in reverse
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char character;

	for (character = 'z'; character => 'a'; character--)
		putchar(character);

	putchar('\n');

	returnt (0);
}
