#include "main.h"
#include <stdio.h>

/**
 * mul - get the product of a and b
 * @a: first variable
 * @b: second variable
 * 
 * Return: product
 */
int mul(int a, int b)
{
	int x;

	x = a * b;
	return (x);
}
